# Clippy configuration for VeloStream
# Allow certain patterns that are common in streaming frameworks

# Allow breaking exported API during development
avoid-breaking-exported-api = true

# Allow some complex patterns for performance-critical code
too-many-arguments-threshold = 10