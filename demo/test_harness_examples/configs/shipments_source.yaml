# Shipments Source Configuration
# For Tier 3 stream-stream join demos

extends: common_kafka_source.yaml

topic:
  name: "test_shipments"

consumer.group: "test_harness_shipments_group"
performance_profile: balanced

datasource:
  schema:
    key.field: shipment_id
