# Trades Source Configuration
# For Tier 2 compound keys demo

extends: common_kafka_source.yaml

topic:
  name: "test_trades"

consumer.group: "test_harness_trades_group"
performance_profile: balanced

datasource:
  schema:
