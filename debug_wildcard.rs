use std::collections::HashMap;

fn main() {
    // Print debug info about the wildcard parsing
    println!("Testing wildcard parsing...");
    
    // Test the path parsing
    let path = "orders[*].amount";
    println!("Parsing path: {}", path);
    
    // This should show what path components we get
}
